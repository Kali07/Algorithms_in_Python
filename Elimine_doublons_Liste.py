#fonction elimine doublons dans Liste

Liste = [1,2,2,1,2,]
def eliminer(Liste) :
    retour = []
    Liste2 = Liste
    for i in range(len(Liste)) :
        if Liste[i] == Liste[] :
            Liste.remove(Liste[i])
    return Liste

print("les doublons sont : ", eliminer(Liste))
